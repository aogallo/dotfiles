service: aws-yaml-lsp-fixture
frameworkVersion: '4'

provider:
  name: aws
  runtime: nodejs20.x
  region: us-east-1

functions:
  hello:
    handler: handler.hello
    events:
      - httpApi:
          path: /hello
          method: get

custom:
  stage: dev
