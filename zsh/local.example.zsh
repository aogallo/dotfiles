# Copy this file to ~/.zshrc.local for personal shell customizations.
# This file is an example only; keep the real local file out of Git.

# Personal project search roots.
# export PROJECT_PATHS=("$HOME/dev/learning" "$HOME/dev/projects" "$HOME/dev/sandbox")

# Auto-start tmux for interactive shells when desired.
# export ZSH_AUTO_START_TMUX=1

# Personal aliases.
# alias work='cd "$HOME/path/to/workspace"'

# Local tool overrides.
# export FZF_DEFAULT_COMMAND='fd --hidden --strip-cwd-prefix --exclude .git'
